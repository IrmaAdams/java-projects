import javax.swing.*;
import java.awt.*;


public class Main {
    public static void main(String[] args) {

        // JFrame = a GUI window to add components to

//        JFrame frame = new JFrame();                // creates an instance of a frame
//        frame.setTitle("My JFrame");                // set screen title
//        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);   // close application (default = hide)
//        frame.setResizable(false);                  // prevent frame from being resizable
//        frame.setSize(420,520);         // set x and y dimensions of the frame using int
//        frame.setVisible(true);                     // makes frame visible
//
//        ImageIcon myLogo = new ImageIcon("logo.jpeg"); // create an image icon
//        frame.setIconImage(myLogo.getImage());                  // set logo (not working)
//        frame.getContentPane().setBackground(Color.green);      // set background colour from default list
//        frame.getContentPane().setBackground(new Color(100,100,100));      // set custom background colour


        //MyFrame myFrame = new MyFrame();
        // or
        new MyFrame();

    }
}